# BitTo サーバー（chaintrace）— 作業を始める前に

暗号資産の詐欺被害者が、盗まれた資金の行き先を追跡するサービスのサーバー。
**読み手は被害に遭った直後の人**で、専門知識は無い。運営は Himesen株式会社。
Railway に自動デプロイ（`git push origin main` で反映）。

## まず読むもの

```
../bitto-mobile/docs/PROJECT-LOG.md   決定事項・過去の落とし穴・未解決
```

`server.js` は約9,700行の1ファイル。**コメントに「なぜそうしたか」が書いてある。**
特に ★ 付きのコメントは、実データで痛い目に遭って書いたもの。消さないこと。

## 何をしている仕組みか

```
1. TXIDを受け取り、チェーンを判定する（BTC/ETH/Polygon/Arbitrum/XRP/TRON）
2. 資金の流れを追い、到達した取引所を特定する
3. 経路と説明を組み立てて返す（★説明文はサーバーが書く。アプリは出すだけ）
4. 有料は報告書PDFとサポートチャットを作る
```

## 触るときの約束

### ★推測で直さない。測ってから直す

時間切れの原因を3回続けて推測で外し、そのたび別の不具合を作った（第5-G節）。
診断画面（`/api/admin/timing`）を作ったら5分で原因が分かった。
**「たぶんここが遅い」で削らない。**

### ★取得できなかったことと、無いことを区別する

TronGridが429を返したのを「送金が無い」と読み、
**資金の終点ではないのに「そこで終わった」と表示していた**（第5-K-2節）。
外部が失敗したら、失敗したと書く。黙って空を返さない。

### ★同じ判断を2箇所に書かない

渡り先のたどり方・送金元の除外・釣り銭の規則で、何度も片方が古くなった。
2箇所に増やしたくなったら、1つにまとめる。

### ★外部への問い合わせは必ず上限時間を持つ

上限が無いと、相手が応答しないとき調査ごと止まる。
利用者には「解析中」のまま何も出ない。**時間切れは誤った答えより悪い。**
`fetchT()` を使う（素の `fetch` は使わない）。

### ★MistTrackの残り回数を無駄にしない

無料調査は1件あたり1回しか外部ラベルを引けない。
過去に「対応していないチェーンなのに枠だけ減らす」「送金元に使う」で
無駄にしていた（第5-K節）。枠を使う場所は8つあり、
**すべてが「対応チェーンか」「出金元でないか」を確かめること。**

### ★経路は切り落とさない

資金が他人の資金と混ざっても、経路は消さずに「どれだけ薄まったか」を添える。
（第4-Z節・第5-H節）

## 秘密の扱い

```
APIキー        .env（ローカル）と Railway Variables（本番）。チャットに貼らない
ADMIN_TOKEN    平文で扱わない。管理者操作は本人が行う
OCRの画像      保存しない（被害者のスクショに残高や個人情報が入る）
生成PDF        ディスクに残さない（メモリのみ・6時間・120件まで）
診断ログ       アドレス・TXID・鍵を残さない（`callTag` が問い合わせ先だけ記録）
利用者の入力    そのままHTMLやURLに入れない（`escHtml` / `encodeURIComponent`）
```

## 情報源

```
BTC     mempool.space（鍵不要・速い）→ 取れなければ Blockchair
EVM     Etherscan v2（chainid切替）
XRP     XRPL RPC（取引）・xrpscan（名前）→ Bithomp（鍵があれば二重化）
TRON    TronGrid（取引・要TRON_API_KEY）・TronScan（名前）
取引所名 MistTrack（BTC/ETH/TRON。★XRPは対象外）
```

**XRPはMistTrackの対象外。** 資金がXRPからTRONへ渡った場合、
外部照会は渡り先のチェーンで判定する（`chainKeyFromName`）。

## 試験

Windows側の scratchpad に約1,280件。`run.sh` で一括実行。
**Mac には無い。** 移す場合は各ファイル先頭のパスを直すこと。
